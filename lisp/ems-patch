diff --git a/lisp/emacspeak-preamble.el b/lisp/emacspeak-preamble.el
index 706ca7a28..1d37cf9a7 100644
--- a/lisp/emacspeak-preamble.el
+++ b/lisp/emacspeak-preamble.el
@@ -182,7 +182,10 @@ FUNNAME to our stored value of ems--interactive-funcname."
           (macroexpand-all
            (macroexp-progn body)
            `((ems-interactive-p         ; new definition
-              . ,(lambda () `(eq ems--interactive-funcname ',funname)))
+              . ,(lambda () `(prog1 (eq ems--interactive-funcname ',funname)
+                          ;; Reset the var to try and avoid misfiring if
+                          ;; it calls itself recursively.
+                          (setq ems--interactive-funcname nil))))
              . ,macroexpand-all-environment)))))
 
 (defun ems-interactive-p ()
